import { ReactNode } from "react";

export type serviceType = {
  icon: ReactNode;
  title: string;
  description: string;
};

export type contryType = {
  image: string;
  name: string;
  offerNumber: string;
};
